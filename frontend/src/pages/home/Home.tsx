import styles from "./Home.module.scss";

const Home = () => {
    return (
        <div className={styles.root}>
            <h1 style={{ textAlign: "center" }}>Welcome</h1>
        </div>
    );
};

export default Home;
